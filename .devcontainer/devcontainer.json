{
  "name": "Norwich dev container",

  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },

  "hostRequirements": {
    "cpus": 4,
    "memory": "16gb"
  },

  "onCreateCommand": "echo \"Downloading the Lean 4 mathematical library...\" && lake exe cache get && lake build +Norwich.Test && echo \"Finished setup! Open a file using the Explorer in the top-left of your screen.\"",

  "customizations": {
    "vscode" : {
      "extensions" : [ "leanprover.lean4" ]
    }
  }
}
